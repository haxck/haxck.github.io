---
const { date } = Astro.props;
const ndate = new Date(date)
---

<time datetime={ndate.toISOString()}>
	{
		ndate.toLocaleDateString('zh-cn', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
